#ifndef _MYMATH_H
#define _MYMATH_H

#include <iostream.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
#include <errno.h>

#define POW_INT_EPS 1e-12

double fact(unsigned x);

/*
 * Calculates double factorial (!!); this function
 * allows for signed values to take "-1" into account
 * which is sometimes generated by functions.
 */
double fact2(int x);

double binomial_prefactor(int s, int ia, int ib, double xpa, double xpb);

int binomial(int a, int b);

double mypow(double base, double exp);

void swap(unsigned &a, unsigned &b);

#ifndef min
#define min(a,b) (((a) < (b)) ? (a) : (b))
#endif

#ifndef max
#define max(a,b) (((a) > (b)) ? (a) : (b))
#endif

#endif